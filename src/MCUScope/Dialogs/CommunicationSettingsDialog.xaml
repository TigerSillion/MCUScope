<Window x:Class="MCUScope.Dialogs.CommunicationSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Communication Settings" Height="200" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Baud Rate (bps):" VerticalAlignment="Center" Margin="0,4"/>
        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="BaudRateComboBox" Margin="8,4" Width="160"
                  HorizontalAlignment="Left" IsEditable="True" IsTextSearchEnabled="False"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="ICS Base Clock (MHz):" VerticalAlignment="Center" Margin="0,4"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="ClockTextBox" Margin="8,4" Width="100"
                 HorizontalAlignment="Left" VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Effective Rate:" VerticalAlignment="Center" Margin="0,4"/>
        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="RateText" Margin="8,4" VerticalAlignment="Center"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,8" FontSize="10" Foreground="Gray"
                   TextWrapping="Wrap"
                   Text="Baud rate is used for UART. ICS base clock is optional for compatibility; effective rate is baseClock/8 if baud rate is empty."/>

        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="OK" Width="80" Margin="4" Click="OnOk" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Margin="4" Click="OnCancel" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
